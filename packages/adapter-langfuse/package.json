{
  "name": "@langcost/adapter-langfuse",
  "version": "0.16.0",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "description": "Langfuse adapter for langcost — ingests production agent traces via the Langfuse API",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vjvkrm/langcost.git",
    "directory": "packages/adapter-langfuse"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "bun test"
  },
  "dependencies": {
    "@langcost/core": "workspace:*",
    "@langcost/db": "workspace:*"
  }
}
